/**
 * Display to the screen the image represented by comma separated string,
 * with dimensions width x height.
 * @param {string} image 
 * @param {int} width 
 * @param {int} height 
 */
const printImage = (image, width, height) => {
    //TODO
    console.log(image);
}

const shadeToRgb = shade => {
    //TODO
    return '#AAAAAA';
}


module.exports = {
    forTest: {
        shadeToRgb,
    },
    printImage,
}
